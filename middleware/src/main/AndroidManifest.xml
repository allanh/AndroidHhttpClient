<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fuhu.middleware">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Google Cloud Message
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <permission android:name="com.fuhu.test.smarthub.permission.C2D_MESSAGE"
        android:protectionLevel="signature" />
    <uses-permission android:name="com.fuhu.test.smarthub.permission.C2D_MESSAGE" />
    -->

    <!-- WIFI P2P
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    -->

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <service android:name="com.fuhu.middleware.service.GoogleRecognizeService" />
        <!--

        <service
            android:name="com.fuhu.middleware.service.gcm.MyGcmListenerService"
            android:exported="false" >
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
            </intent-filter>
        </service>

        <service
            android:name="com.fuhu.middleware.service.gcm.MyInstanceIDListenerService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.android.gms.iid.InstanceID" />
            </intent-filter>
        </service>

        <service android:name="com.fuhu.middleware.service.gcm.RegistrationIntentService"
            android:exported="false" />

        <receiver
            android:name="com.google.android.gms.gcm.GcmReceiver"
            android:exported="true"
            android:permission="com.google.android.c2dm.permission.SEND" >
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
                <category android:name="com.fuhu.test.smarthub.gcm" />
            </intent-filter>
        </receiver>
-->
        <receiver android:name="com.fuhu.middleware.receiver.SaveToCacheReceiver" >
            <intent-filter>
                <action android:name="com.fuhu.test.smarthub.action.saveYoCache" />
                <action android:name="com.fuhu.test.smarthub.action.resetYoCache" />
                <action android:name="com.fuhu.test.smarthub.action.renewYoCache" />
            </intent-filter>
        </receiver>

        <!--
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDvPan9UFVL950p-okbJ-XCFKAnHzjn0Hk" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
            -->
    </application>

</manifest>
